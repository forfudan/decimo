"""The version stamped into the wheel.

`pixi run release` overwrites this with `0.1.0.devYYYYMMDDHHMMSS` before it
builds, so what is committed here is only the fallback that a source install
or an editable install sees.
"""

__version__ = "0.1.0.dev0"
